{
  "name": "phonebook",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../../part2/exercises/phonebook && npm run build && cp -r dist ../../../part3/exercises/phonebook",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongoose": "^9.1.1",
    "morgan": "^1.10.1"
  }
}
